Repository contains all current files for EE3613 HW4.

Group Members: Mason Kabat, Carson Bartholic

Each module has been out in its own file in order to make the repository more organized.

code.v contains all of the modules put together so that it can easily be ran.

test.v also contains all of the modules put together, this file was used to make slight changes to compare results with those of code.v

testBench.v contains test cases for the 16bitALU module.

code_Siqin.v contains the code given in class(that does not work) along with some other things

SignExtender.v contains the code for the sign extender along with its test bench

ALU32waveForm folder contains the wave form from the 32 bit alu tests and explanation